# ============================================
# Uniclima Vendure - Environment Variables
# ============================================
# Copy this file to .env and adjust values as needed
# For production, use strong passwords and external services!
# ============================================

# -----------------------------
# Environment Configuration
# -----------------------------
# Options: dev, staging, production
APP_ENV=dev
NODE_ENV=development

# -----------------------------
# Server Configuration
# -----------------------------
PORT=3001

# -----------------------------
# PostgreSQL Database
# -----------------------------
# For Docker development, use 'db' as host
# For production, use your managed database host
DB_HOST=db
DB_PORT=5432
DB_NAME=vendure
DB_USERNAME=vendure
# IMPORTANT: Change this password in production!
DB_PASSWORD=vendure

# -----------------------------
# Security
# -----------------------------
# IMPORTANT: Generate a secure random string for production!
# Use: openssl rand -base64 32
COOKIE_SECRET=yclAsZIhRw2CWAVDT4GbwQ

# -----------------------------
# Superadmin Credentials
# -----------------------------
# IMPORTANT: Change these in production!
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=superadmin

# -----------------------------
# Asset Storage (Optional)
# -----------------------------
# For production, consider using S3 or similar
# ASSET_STORAGE_TYPE=s3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=eu-west-1

# -----------------------------
# Email (Optional)
# -----------------------------
# For production, configure SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# EMAIL_FROM=noreply@uniclima.com

# -----------------------------
# Frontend URL
# -----------------------------
# Used for email links and CORS
FRONTEND_URL=http://localhost:3000

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# Before deploying to production:
# [ ] Generate new COOKIE_SECRET: openssl rand -base64 32
# [ ] Set strong DB_PASSWORD (min 16 characters)
# [ ] Change SUPERADMIN_USERNAME and SUPERADMIN_PASSWORD
# [ ] Configure external PostgreSQL host
# [ ] Set APP_ENV=production and NODE_ENV=production
# [ ] Configure asset storage (S3 recommended)
# [ ] Set up SMTP for emails
# [ ] Update FRONTEND_URL to production domain
# ============================================
